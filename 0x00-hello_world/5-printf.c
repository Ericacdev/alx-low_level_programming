#include <stdio.h>

/**
 *  main - a program that prints with proper  
 *   grammar using the function printf 
 *    but the outcome is a piece of art,
 *     Return: 0 
 */      
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
